name: fukuoka_trip
description: 福岡旅遊 App 專案
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.5
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5

flutter:
  uses-material-design: true
